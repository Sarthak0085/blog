/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['picsum.photos', 'example.com', 'res.cloudinary.com'], // Add the hostname here
  },
};

export default nextConfig;
