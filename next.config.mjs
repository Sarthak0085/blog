/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['picsum.photos', 'example.com', 'res.cloudinary.com', 'www.freecodecamp.org', 'miro.medium.com', 'unsplash.com', 'imgs.search.brave.com'], // Add the hostname here
  },
};

export default nextConfig;
